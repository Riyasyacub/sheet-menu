<div>
  <% @menu.each do |category, items| %>
    <div class="container mx-auto">
      <div class="text-3xl font-semibold text-gray-900"><%= category %></div>
      <div class="flex flex-wrap">
        <%= render partial: 'item', collection: items, as: :item %>
      </div>
    </div>
  <% end %>
</div>